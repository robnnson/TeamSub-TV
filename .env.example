# Application
NODE_ENV=development
PORT=3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=signage
DB_PASSWORD=signage_password
DB_DATABASE=signage_cms

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Secrets (Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your-jwt-secret-here-replace-with-random-string
JWT_REFRESH_SECRET=your-jwt-refresh-secret-here-replace-with-random-string

# Encryption Key (Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=your-encryption-key-here-replace-with-random-string

# CORS
CORS_ORIGIN=http://localhost:3001

# Admin Portal
ADMIN_PORT=3001
REACT_APP_API_URL=http://localhost:3000/api

# External API Keys (These will be encrypted and stored in database)
OPENWEATHER_API_KEY=9fcfc0149fef9015a6eaba1df22caf5b
WMATA_API_KEY=d1df6a70755d42398beefaf6ee90a662
TOMTOM_API_KEY=5mbANdobpExgtq28V5rxlSylvDx87ulH
